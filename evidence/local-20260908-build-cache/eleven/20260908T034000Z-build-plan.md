# Cold build and exact runtime reuse

Owner: Eleven. Work unit: local-20260908-build-cache.
Base: 04418b000fdcb649f3e65b72d7d7f714d8250f83.
Authority: user requested pipeline acceleration, then explicitly cold-build acceleration.
Live broadcast fixes remain paused during this work.

## Observed baseline

Run 34182546525, build job 101924471719 (windows-2022):
- Whole build job: 03:10:24--03:19:41 UTC = 557 seconds.
- Build step: 03:11:16--03:19:22 = 486 seconds, with dependencies cache hit.
- Pulsar native build phase: 03:14:33--03:19:22, about 289 seconds.
- Upstream enables /MP; top-level Pulsar does not. MSBuild project parallelism
  alone does not parallelize translation units within large projects.
- Upstream browser DLL/helper are compiled, then deleted by build-win.ps1.
- Pulsar owns browser_source; upstream browser POST_BUILD currently supplies CEF files.

## Criteria, risks and validation

| Criterion | Risk | Check | Evidence required |
|---|---|---|---|
| Faster cold build | Timing noise / CPU or memory oversubscription | Hosted CI with native cache miss, identical runner class | Actual build step duration; no claimed estimate |
| Preserve full browser runtime | Missing CEF runtime files after upstream browser disabled | Native manifest required files; compare baseline/new CEF file hashes; offline CTest | Complete CEF payload and passing required checks |
| Native compiler changes preserve behavior | /MP or PCH ordering/macro issues | Full compilation, binary gate and complete offline probes | All required CI checks |
| Exact cache only | Stale native input or wrong absolute CTest paths | Unit tests for native files/gitlink/version/workflow/toolchain/workspace invalidation | Seven local tests and CI contract job |
| No tests skipped on reuse | Cached test results/scripts | Cache contains only existing artifact payload + manifest; re-run same PR after cold success | Cache hit, build skipped, downstream tests freshly run |
| Safe producer provenance | Wrong/incomplete artifact | SHA-256 manifest verification before upload on both cold and warm path | Producer SHA/run recorded, full file inventory verified |

## Implementation boundaries

- /MP2 is bounded per Pulsar project, configurable via PULSAR_COMPILE_JOBS.
- Precompile only nlohmann/json.hpp in pulsar-websocket; no per-source JSON
  configuration macros exist in the current sources.
- Do not compile unused upstream browser in headless -Full; retain GuiBuild.
- Stage exactly the same pinned CEF file list/locales from pulsar-browser.
- Native cache conservatively includes every tracked input except docs/evidence,
  selected root documentation and three reviewed interpreted probe files.
- Cache does not contain sources, runtime credentials/state, or test results.
- GitHub cache branch isolation is retained; no cross-PR artifact retrieval.

Rollback: revert this change. Native cache key includes workflow/build files, so
any rollback invalidates the exact cached outputs automatically.

Initial validation: 7 unittest cases passed locally; git diff --check passed.
CI timing, native build and warm-cache execution are still pending.
