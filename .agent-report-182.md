AGENT_REPORT

Role: forge
Agent-Thread: WU-pulsar-182-outputfailed-20260810
Work-Unit: WU-pulsar-182-outputfailed-20260810
Issue: 182
Result: READY (CI in progress, not yet fully green)

PR: https://github.com/ZabLaboratory/Pulsar/pull/193
Branche: forge/182-outputfailed-reason-class, commit 332b9cb6652ee4cff4d9cb7eca43fcf57762d03b,
checkpoint status=pushed.

Implémentation : hookOutputSignals (pulsar-frontend-stub.cpp) extrait code/last_error du
calldata "stop" ; DestinationRegistry::start/start_all (plugin-main.cpp) émettent le même
événement sur refus synchrone. Classification centralisée dans un nouveau header-only
plugins/pulsar-output-classify (même patron que pulsar-nv-secure-load), pont cross-plugin
via proc_handler global (frontend-stub -> vendor "pulsar" de multi-stream). Table RC5 +
jeu fermé documentés dans docs/PROTOCOL.md. Regression : tests/output-classify-probe (CTest
pur, sans OBS), imprime un payload JSON par scénario (7 classes + RC7).

CI (PR #193) : lint / contract-tests / deps-audit / lockfile-check / codeowners-check
= PASS. `build pulsar.exe (-Full)` en cours (~30 min, Windows). offline-probes (CTest,
donc mon nouveau test) pas encore lancé -- dépend du build. Pas de build local possible
(pas de MSVC dans cet environnement, cl.exe absent ; build complet = obs-deps+Qt6+CEF,
Windows CI uniquement).

Écarts/hypothèses (détaillées dans le corps de la PR) :
- code -3 (OBS_OUTPUT_INVALID_STREAM) classé ingest_dropped plutôt qu'ingest_unreachable :
  jugement, pas une certitude.
- disconnected_local : déclenché uniquement par output local (virtualcam) faute de
  trigger déterministe côté réseau dans le jeu (code, last_error).
- OBS_OUTPUT_NO_SPACE (-7) -> unknown (aucune classe du jeu fermé ne correspond).
- phase ("start"/"active") : valeurs non énumérées littéralement par l'ADR, choix de
  conception documenté.
- Pont proc_handler cross-plugin jamais exercé en runtime réel dans cet environnement ;
  repose sur le mécanisme déjà utilisé par obs-websocket-api.h lui-même, mais
  offline-probes (CI) est le premier exercice réel.

Table RC5 : aucune signature Prism orpheline (table complète dans docs/PROTOCOL.md).

Surface sensible : aucune (pas d'auth/secrets/réseau nouveaux ; événement additif,
consommé par personne encore côté Prism).
