{
  "_artefact": "Frozen coverage list for scripts/probe-capability-contract.py.",
  "_why": [
    "ADR Prism 026 §3.3 palier 3 / Pulsar issue #121 resolution criterion 3: the",
    "coverage of the capability-contract gate is an EXPLICIT, VERSIONED list, not",
    "whatever the probe happens to walk on the day. The probe cross-checks itself",
    "against this file and FAILS on drift in either direction:",
    "  - a listed subject that was NOT driven  -> the gate silently lost coverage;",
    "  - a driven subject that is NOT listed   -> this artefact went stale.",
    "Removing a line from `subjects` therefore cannot happen by accident: it must",
    "also be removed from the probe, which makes it a visible diff. That diff",
    "failing REVIEW is the human control the ADR asks for -- it is deliberately",
    "not a test (a test cannot tell a legitimate retirement from an amputation)."
  ],
  "_denominator": {
    "advertised_request_types": 137,
    "source": "docs/PROTOCOL.md -- the count Pulsar advertises as its v5 surface.",
    "note": [
      "GetVersion on a -Full build reports a larger availableRequests array (151)",
      "because it includes the pulsar: vendor requests and the handlers upstream",
      "registers but PROTOCOL.md does not advertise. 137 stays the denominator so",
      "the ratio keeps meaning the same thing across builds."
    ]
  },

  "subjects": {
    "General": ["GetVersion", "GetStats"],
    "Scenes": ["CreateScene", "SetCurrentProgramScene", "RemoveScene"],
    "Inputs": ["CreateInput", "SetInputSettings", "RemoveInput"],
    "SceneItems": ["SetSceneItemTransform", "SetSceneItemEnabled"],
    "Filters": [
      "CreateSourceFilter",
      "SetSourceFilterSettings",
      "SetSourceFilterEnabled",
      "RemoveSourceFilter"
    ],
    "Transitions": ["SetCurrentSceneTransition"],
    "Stream": ["SetStreamServiceSettings", "StartStream"],
    "Record": ["StartRecord", "PauseRecord", "ResumeRecord", "StopRecord"],
    "ReplayBuffer": ["StartReplayBuffer", "SaveReplayBuffer", "StopReplayBuffer"],
    "VirtualCam": ["StartVirtualCam", "StopVirtualCam"],
    "Outputs": ["GetOutputList", "GetOutputStatus", "StartOutput"],
    "StudioMode": [
      "SetStudioModeEnabled",
      "SetCurrentPreviewScene",
      "TriggerStudioModeTransition"
    ],
    "Canvases": ["GetCanvasList"]
  },

  "cross_check_requests": [
    "GetCanvasList",
    "GetCurrentProgramScene",
    "GetCurrentSceneTransition",
    "GetInputList",
    "GetInputSettings",
    "GetLastReplayBufferReplay",
    "GetOutputList",
    "GetOutputStatus",
    "GetRecordStatus",
    "GetReplayBufferStatus",
    "GetSceneItemId",
    "GetSceneItemEnabled",
    "GetSceneItemList",
    "GetSceneItemTransform",
    "GetSceneList",
    "GetSceneTransitionList",
    "GetSourceFilterKindList",
    "GetSourceFilter",
    "GetSourceFilterList",
    "GetStreamServiceSettings",
    "GetStreamStatus",
    "GetStudioModeEnabled",
    "GetVideoSettings",
    "GetVirtualCamStatus"
  ],
  "_cross_check_note": [
    "Requests used ONLY as the independent re-query for someone else's verdict.",
    "They are exercised for real (a broken one breaks the verdict it backs), but",
    "they are not the subject under test, so they are not gated by name."
  ],

  "deliberately_uncovered": {
    "Profiles / SceneCollections": "Pulsar is headless and boots one implicit profile+collection; Prism never drives these. No consumer, no gate -- covering them would test upstream, not Pulsar.",
    "Hotkeys": "No input device in a headless service; TriggerHotkeyByName has no consumer in Prism. Same reasoning as above.",
    "MediaInput cursor": "SetMediaInputCursor / OffsetMediaInputCursor / TriggerMediaInputAction need a real ffmpeg_source with a media file; the fixture cost is real and no Prism path drives them today. Next candidate after the ones below.",
    "SplitRecordFile / CreateRecordChapter": "Both are format-conditional (fragmented mp4 / hybrid mp4) and would need their own record-format fixture. Left out of this pass on cost, not on principle.",
    "SetSceneItemIndex / SetSceneItemBlendMode / SetSceneItemLocked": "Same family as the two SceneItem subjects already gated; adding them is mechanical and is exactly the kind of widening the ADR calls a separate job.",
    "Projectors / Dialogs (Open*Projector, Open*Dialog)": "Qt frontend surface. Pulsar runs QT_QPA_PLATFORM=minimal with no UI; these are structurally inapplicable.",
    "pulsar: vendor requests": "Multi-stream (pulsar:StartDestination) and pulsar:GetCapabilities have their own dedicated probes (probe-multi-stream.py, live-test.yml). Not re-litigated here."
  },

  "open_findings_routed_out": {},
  "_closed_findings": {
    "RemoveInput reports success and removes nothing": [
      "OPENED 2026-07-27 Forge/#121, CLOSED 2026-07-27 by issue #129.",
      "Root cause was the one Atlas confirmed: obs_source_remove() only flags",
      "the source and fires the global `source_remove` signal; the scene items",
      "holding the last refs are dropped by obs_scene_prune_sources(), which",
      "libobs calls only from scene_video_render -- never for a scene it is not",
      "rendering. obs-studio closes that loop in the FRONTEND; pulsar-frontend-stub",
      "did not. It now connects `source_remove` in setup() and prunes every scene",
      "in two phases (collect refs during obs_enum_scenes, prune after).",
      "RemoveInput is a GATED subject from now on (Inputs family): the probe",
      "removes the input from a scene that is NOT the program scene -- exactly",
      "the condition under which the defect existed -- and requires it gone from",
      "both GetInputList and GetSceneItemList."
    ],
    "PauseRecord before the muxer's first bytes wedges record + replay": [
      "OPENED 2026-07-27 Forge/#121, CLOSED 2026-07-27 by issue #130.",
      "Confirmed upstream (libobs obs-output.c get_closest_v_ts + obs-encoder.c",
      "video_pause_check_internal): the pause window is computed from an encoder",
      "timestamp that is still 0 before the first encoded frame, so the pause",
      "never lifts. Patching libobs is out of the fork's mandate, so the refusal",
      "lives in the only layer that can name a cause: RequestHandler_Record.cpp",
      "refuses PauseRecord / ToggleRecordPause at outputBytes == 0 with",
      "InvalidResourceState (604), and refuses both on an inactive output with",
      "OutputNotRunning (501) -- the #120-family Success()-lie they also carried.",
      "Now GATED (Record family, PauseRecord): the probe drives the off-air",
      "pause and the 0-byte pause and requires an explicit refusal for each.",
      "Driving them is only safe BECAUSE of the refusal -- before #130 that",
      "sequence bricked the rest of the run."
    ],
    "SetStreamServiceSettings has no effect on the v5 stream output": [
      "OPENED 2026-07-27 Forge/#121, CLOSED 2026-07-27 by issue #131.",
      "Atlas ruled option 1 (wire the v5 path), applying the already-ratified",
      "`Approach A` doctrine (pulsar-multi-stream/src/plugin-main.cpp:9-16: the",
      "v5 StartStream/StartRecord path keeps working for Stream Deck / Companion",
      "/ Streamer.bot; multi-destination is purely additive). The whole defect was",
      "a missing obs_output_set_service() before obs_output_start() -- an",
      "rtmp_output with a NULL service is refused by libobs on the first line of",
      "obs_output_start. Fixed in obs_frontend_streaming_start, together with the",
      "honesty corollary: OBS_FRONTEND_EVENT_STREAMING_STARTING is now emitted",
      "only AFTER a successful obs_output_start (it used to fire unconditionally,",
      "putting a STARTING event on the wire for a start that never happened).",
      "That ordering is what makes the gate deterministic offline: the probe",
      "requires a StreamStateChanged/OBS_WEBSOCKET_OUTPUT_STARTING event, which",
      "is impossible to produce without the service binding, instead of an",
      "outputActive read that a still-connecting rtmp output can never satisfy."
    ]
  },
  "_closed_findings_note": [
    "Kept, not deleted: an emptied `open_findings_routed_out` with no trace of",
    "what used to be in it is indistinguishable from a list that was quietly",
    "truncated. Each entry names the issue that closed it and where the gate",
    "went instead."
  ]
}
