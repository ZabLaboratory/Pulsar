# build-win.ps1 — Windows build entry point for Pulsar.
#
# Phase 1 deliverable:
#   - check toolchain (VS 2022 Build Tools, CMake, Yarn 4)
#   - init submodule + apply patches
#   - configure + build with PULSAR_BUILD_UPSTREAM=ON
#   - package into dist/pulsar-win-x64.zip
#
# Stub for now.

Write-Host "Pulsar Windows build pipeline not yet implemented (Phase 1)."
Write-Host "See docs/DEVELOPMENT.md for the planned build steps."
exit 1
