AGENT_CHECKPOINT

Role: forge
Agent-Thread: WU-pulsar-213-mklink-probe-fix-v2-20260811
Work-Unit: WU-pulsar-213-mklink-probe-fix-v2-20260811
Issue: #213
PR: #216

Status: rebased forge/213-mklink-probe-fix on main (fe9ef34, includes #221 PULSAR_CHECK
macro + #218 ctest --output-junit). Resolved the single conflict in
tests/dir-hardening-probe/dir-hardening-probe-test.cpp by keeping both: main's
PULSAR_CHECK macro/definitions, and #213/#216's new negative cases (gesture 3
different-owner refusal, gesture 4 post-rename substitution detection, hard-fail
reparse-point case). Converted the 6 remaining raw assert() calls the branch had
added (including the two effectful ones: create_protected_temp_file, MoveFileExA)
to PULSAR_CHECK. No assert()/<cassert> remain in the file.

Local proof (standalone MSVC compile of dir-hardening-probe-test.cpp +
dir-hardening.cpp, no OBS dependency needed):
- Unprivileged run: gesture 2 (reparse) executes for real (mklink /J rc=0,
  junction refusal confirmed). Gestures 3/4 print explicit NOT RUN with reason
  "SeRestorePrivilege is not held by this runner's token (ERROR_NOT_ALL_ASSIGNED)"
  -- named privilege gap, not a generic setup-failure string.
- Regression-injection proof (scratch copy only, never the committed file):
  disabled the reparse-point refusal in a scratch dir-hardening.cpp copy,
  rebuilt against the UNMODIFIED committed test file -- PULSAR_CHECK correctly
  fires ("CHECK FAILED: !pulsar_dir::create_directory_hardened(junctions, "test")
  (dir-hardening-probe-test-copy.cpp:203)"), exit 1. Proves the negative case is a
  real gate, not decoration.
- Could not obtain SeRestorePrivilege locally (UAC-filtered admin token, schtasks
  /RU SYSTEM denied) to force-exercise gestures 3/4 for real on this box. CI runs
  on windows-2022 GitHub-hosted runners (pipeline.yml); relying on CI's own token
  to confirm whether gestures 3/4 execute for real there or also land NOT RUN --
  either way the log now names the reason explicitly, never a silent pass.

Pushed d875fef to origin/forge/213-mklink-probe-fix (force, rebase). Checkpoint
status=clean. PR #216 CI running (secret-scan/lint IN_PROGRESS at last check,
5 other checks green).
