# CI docs-only gate test (temporary)

Throwaway file to prove #205's `changes` job correctly reports the 4
required checks as skipped/green on a docs-only diff, without triggering
the MSVC build. This file and its branch are deleted once the checks are
observed; no merge.
