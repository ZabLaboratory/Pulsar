# pulsar-headless plugin — service-mode entry for Pulsar.
#
# Phase 2 wiring will add:
#   - libobs find_package() against the built upstream
#   - executable target pulsar-headless
#   - signal-bridge sources
#
# Kept as a stub so the plugin slot exists in the source tree.

cmake_minimum_required(VERSION 3.28)
project(pulsar-headless LANGUAGES CXX)

message(STATUS "pulsar-headless: not yet implemented (Phase 2)")
