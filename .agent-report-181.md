AGENT_REPORT

Role: forge
Agent-Thread: forge-181-acl-create-new
Work-Unit: WU-pulsar-181-acl-hardening-20260810
Issue: 181 (Bastion follow-up on PR #191)
Branch: forge/181-acl-create-new
PR: https://github.com/ZabLaboratory/Pulsar/pull/194
Commit: 2c6d1de06bafd816228fe747116f748ab67c60bd

Result: READY (pending full CI build job)

Changes:
- plugins/pulsar-headless/main.cpp: seed_websocket_config now uses
  CreateFileA(CREATE_NEW, ...) with a pre-built, protected DACL passed via
  SECURITY_ATTRIBUTES (build_current_user_only_security_attributes),
  replacing the old ofstream(trunc) + post-hoc SetNamedSecurityInfoA
  (restrict_config_file_to_current_user, removed). Pre-existing-file
  handling: owner==current user -> DeleteFileA + retry CREATE_NEW
  (ordinary reseed); owner!=current user -> refuse to write the password,
  fail closed, log anomaly (R3 squat case).
- scripts/run-probes.ps1: new ACL probe after PULSAR_READY (Phase 2),
  asserts config.json DACL is protected + restricted to current account.

Criteria -> evidence:
- C3 (fix required): DONE — atomic CREATE_NEW + pre-built DACL closes
  R1/R2 (post-hoc restriction race) and R3 (CREATE_ALWAYS squat). Code:
  main.cpp seed_websocket_config + build_current_user_only_security_attributes
  + file_owned_by_current_user.
- C4 (fix required): DONE — real probe added (run-probes.ps1, Phase 2 ACL
  block); comment no longer claims a nonexistent safety net.
- C6 (verification): OPEN, not verifiable from this session — no real
  install-dir (bin/64bit/) context nor second local Windows account
  available. Declared as open risk in the PR body, same as Bastion's
  original report.

Commands run / results:
- MSVC syntax+type compile check (cl.exe /c /std:c++17 /Zc:__cplusplus
  /EHsc /permissive-, same include dirs/defines as the generated
  pulsar-headless.vcxproj, against the real obs/Qt/Windows-SDK headers
  from the canonical checkout's already-fetched upstream/): exit 0, no
  errors/warnings.
- PowerShell AST parse of run-probes.ps1
  ([...Parser]::ParseFile): no syntax errors.
- Did NOT run the full OBS-fork cmake build/ctest locally: the only
  configured build/ dir is bound to the canonical checkout's absolute
  source paths (not this worktree), and a from-scratch build (submodule
  init + CEF fetch + full OBS compile) is multi-hour, out of scope here.

CI observed on PR #194 (gh pr checks, ~5 min after push):
- lint, deps-audit, secret-scan, lockfile-check, codeowners-check,
  contract tests (scene_control): PASS
- build pulsar.exe (-Full) [runs the ACL probe via run-probes.ps1 in
  ctest]: PENDING at time of this report — long-running Windows job, not
  yet resolved. NOT claimed green; verify before merge.

Risks / gaps:
- C6 open (see above), matches Bastion's own unresolved item.
- Legitimate-reseed assumption (owner==current user -> safe to delete)
  not exercised beyond the new probe / a single boot; no multi-boot
  regression test added.
- docs/adr/005-go-live-failure-diagnosability.md (R9 note) not updated —
  out of scope for a minimal code fix, flagged in PR body for Vigil/Bastion.

No merge performed (no-merge tier). No secrets touched or logged.
